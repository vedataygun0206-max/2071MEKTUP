export default {async fetch(request,env){const u=new URL(request.url);if(request.method==='GET'&&u.pathname==='/api/health')return new Response(JSON.stringify({ok:true,service:'2071 Mektup',now:new Date().toISOString()}),{headers:{'content-type':'application/json'}});return new Response(JSON.stringify({ok:false,error:'Endpoint hazırlanıyor'}),{status:404,headers:{'content-type':'application/json'}})}};
